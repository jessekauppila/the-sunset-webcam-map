import { RulesLab } from './RulesLab';

export const metadata = { title: 'solo2 rules lab' };

/** The solo2 ordering rules drawn as a sieve over a textbook pool (rules-lab spec). No data, no sign-in. */
export default function RulesLabPage() {
  return <RulesLab />;
}
